interface:
  display_name: "Kanban"
  short_description: "Inspect an optional file-backed Kanban board"
  default_prompt: "Use $kanban to inspect the optional file-backed Kanban board in this repository."
