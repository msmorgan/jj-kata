interface:
  display_name: "Kanban"
  short_description: "Use the optional file-backed ticket framework"
  default_prompt: "Use $kanban to inspect the optional file-backed work-item board in this repository."
